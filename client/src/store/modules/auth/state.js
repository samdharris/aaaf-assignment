export default {
    /**
     * Flag indicating if we're currently authenticating the user on the login form.
     */
    isAuthenticating: false,
    /**
     * Stores server side validation error messages.
     */
    errors: {},
    /**
     * Stores the currently logged in user.
     */
    currentUser: {}
};
